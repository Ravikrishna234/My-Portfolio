// const images = require.context('../../assets',, false, /\.(png|jpe?g|svg)$/);)
// const images = require.context('../../assets',, false, /\.(png|jpe?g|svg)$/);
// const images = require.context('../../assets',false, /\.(png|jpe?g|svg)$/);
// const imagePaths = images.keys().map(images);
// export default imagePaths;

// import {ProjectImage} from '../../assets/Project-1-Image-1.png';

// export {ProjectImage};
// const images = require.context('../../assets',, false, /\.(png|jpe?g|svg)$/);)
// const images = require.context('../../assets',, false, /\.(png|jpe?g|svg)$/);
// const images = require.context('../../assets',false, /\.(png|jpe?g|svg)$/);
// const imagePaths = images.keys().map(images);
// export default imagePaths;

// import {ProjectImage} from '../../assets/Project-1-Image-1.png';

// export {ProjectImage};
